# 公开前最终复核清单

版本：`v1.0.03`

## 1. 复核目标

对当前 `D:\ABUMeta` 的 Git 已跟踪文件做最后一轮公开前复核，回答两个问题：

1. 当前已跟踪文件里是否还残留明显不该公开的内容。
2. 哪些文件虽然可以公开，但应在 GitHub 首发时附带说明。

## 2. 复核结论

### 2.1 当前未发现的高风险项

本次复核未发现以下明显问题：

- 主开发仓数据库被带入公开仓库
- 缓存、日志、报告、抓取结果被 Git 跟踪
- `.env` 或真实密钥被提交
- 本地虚拟环境被 Git 跟踪
- Windows 快捷方式继续被 Git 跟踪

### 2.2 当前可以公开的主要内容

- `src/`：核心源码
- `config/`：必要静态配置
- `tests/test_smoke_demo.py`：最小 smoke 测试
- `docs/`：精简文档
- `data/schemas/foreign_trade_warning_report.schema.json`：公开 schema
- 根目录许可证、版本、第三方说明、更新日志、社区文件

### 2.3 当前仍需说明但可公开的内容

- `config/site_templates.json`
  - 含第三方目标站点 URL 与抓取模板
  - 可以公开，但应视为“公开规则与适配逻辑”，不是数据结果
- `config/external_risk_catalog.json`
- `config/external_risk_catalog_sanctions.json`
  - 含外部镜像/示例 URL
  - 可以公开，但建议说明这是规则/镜像源配置，不附带运行结果
- `requirements-optional.txt`
  - 含浏览器抓取、向量检索等增强依赖
  - 应继续在 README 和 `THIRD_PARTY_NOTICES.md` 中说明

## 3. Git 已跟踪文件分组复核

### 3.1 仓库治理与说明文件

以下文件适合公开：

- `README.md`
- `READM.MD`
- `LICENSE`
- `THIRD_PARTY_NOTICES.md`
- `开源许可证建议.MD`
- `更新日志.MD`
- `GITHUB_REPOSITORY_METADATA.MD`
- `RELEASE_NOTES_v1.0.02.MD`
- `RELEASE_NOTES_v1.0.03.MD`
- `CONTRIBUTING.md`
- `SECURITY.md`
- `.github/ISSUE_TEMPLATE/*`
- `.github/PULL_REQUEST_TEMPLATE.md`

### 3.2 运行与安装入口

以下文件适合公开：

- `install_demo_env.bat`
- `install_optional_extras.bat`
- `run_demo.bat`
- `一键安装依赖.bat`
- `安装可选增强依赖.bat`
- `运行演示.bat`

说明：

- 这些属于公开版的 Windows 入口脚本
- 本地 `.lnk` 快捷方式已经从 Git 跟踪中移除

### 3.3 版本与元数据文件

以下文件适合公开：

- `VERSION`
- `version.json`
- `src/__init__.py`

### 3.4 配置与规则文件

以下文件可以公开，但建议继续在文档中说明用途：

- `config/settings.py`
- `config/constants.py`
- `config/site_templates.json`
- `config/external_risk_catalog.json`
- `config/external_risk_catalog_sanctions.json`

### 3.5 data 目录

当前 Git 跟踪的 `data/` 内容是安全的：

- `data/README.md`
- `.gitkeep`
- `data/schemas/foreign_trade_warning_report.schema.json`

未发现数据库、缓存结果、报告产物被跟踪。

## 4. README 对外性复核

### 4.1 当前已满足

- 说明了项目是什么
- 说明了如何安装
- 说明了如何运行
- 说明了如何测试
- 说明了依赖如何拆分
- 指向了许可证、第三方说明、更新日志、GitHub 社区文件

### 4.2 当前仍有的边界

- README 已足够首发，但仍是“演示版导向”，不是完整产品文档
- 如果后续要面向更多外部开发者，可以再补：
  - 架构图
  - 模块清单
  - FAQ
  - 截图或 GIF

## 5. 最终公开判断

当前 `D:\ABUMeta` 已达到“可推 GitHub 首发”的条件。

前提是：

- 继续保持 `.env`、数据库、缓存、日志、报告、抓取结果不进入 Git
- 后续如果新增第三方依赖，必须同步更新 `THIRD_PARTY_NOTICES.md`
- 后续如果新增运行数据样本，必须先确认数据是否可公开

## 6. 结论

当前 Git 已跟踪文件中，未发现明显“你不该公开但已经被跟踪”的高风险内容。

仍需对外说明但可以公开的主要内容是：

- 第三方站点模板与规则配置
- 可选增强依赖
- 外部服务接入方式

这些说明当前已经分别落在：

- `README.md`
- `THIRD_PARTY_NOTICES.md`
- `GITHUB_REPOSITORY_METADATA.MD`
