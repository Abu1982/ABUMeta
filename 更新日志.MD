# 更新日志

## v1.0.04
- 时间：2026-03-18 23:20
- 类型：GitHub 首发收口 / 公开发布整理
- 内容：
  - 收紧公开发布边界，统一整理首发步骤、仓库设置建议与发布后复核顺序
  - 同步更新公开说明文档、版本文件与更新日志
  - 建立新的 Git 标签回滚锚点 `v1.0.04`
- 验证结果：
  - 使用副本虚拟环境运行 `tests/test_smoke_demo.py` 通过，结果为 `4 passed`
  - Git 工作区可收敛为干净状态并可创建新标签
- 版本文件：
  - `VERSION`
  - `version.json`
  - `src/__init__.py`

## v1.0.03
- 时间：2026-03-18 22:56
- 类型：首发前最终复核 / 公开仓清理
- 内容：
  - 新增 `.env.example`，补齐公开仓库的环境变量示例入口
  - 继续收紧 `.gitignore`，增加 `.env.local`、`.vscode/`、`.idea/` 等忽略规则
  - 补齐公开发布说明与发布前最终复核内容
  - 同步更新 `README.md`、版本文件与更新日志
  - 建立新的 Git 标签回滚锚点 `v1.0.03`
- 验证结果：
  - 使用副本虚拟环境运行 `tests/test_smoke_demo.py` 通过，结果为 `4 passed`
  - Git 工作区可收敛为干净状态并可创建新标签
- 版本文件：
  - `VERSION`
  - `version.json`
  - `src/__init__.py`

## v1.0.02
- 时间：2026-03-18 22:40
- 类型：发布前最终审计 / GitHub 元数据收口
- 内容：
  - 补齐仓库简介文案、Topics 建议与首发 Release 说明
  - 继续收紧 `.gitignore`，增加本地快捷方式、缓存、覆盖率和临时文件忽略规则
  - 明确 Windows 快捷方式仅用于本地双击体验，不作为公开仓库文件分发基线
  - 同步更新 `README.md`、版本文件与版本说明
  - 建立新的 Git 标签回滚锚点 `v1.0.02`
- 验证结果：
  - 使用副本虚拟环境运行 `tests/test_smoke_demo.py` 通过，结果为 `4 passed`
  - Git 工作区可收敛为干净状态并可创建新标签
- 版本文件：
  - `VERSION`
  - `version.json`
  - `src/__init__.py`

## v1.0.01
- 时间：2026-03-18 18:22
- 类型：首发收口 / GitHub 发布准备
- 内容：
  - 将依赖拆分为 `requirements-demo.txt`、`requirements-optional.txt`、`requirements-dev.txt`
  - 保留 `requirements.txt` 作为默认入口并指向 `requirements-demo.txt`
  - 更新安装脚本，使默认安装更贴近 GitHub 演示使用场景
  - 补齐 GitHub 首发协作文件：`CONTRIBUTING.md`、`SECURITY.md`、Issue 模板、PR 模板
  - 同步 `README.md` 的版本与依赖说明
  - 建立新的 Git 标签回滚锚点 `v1.0.01`
- 验证结果：
  - 使用副本虚拟环境运行 `tests/test_smoke_demo.py` 通过，结果为 `4 passed`
  - Git 工作区可收敛为干净状态并可创建新标签
- 版本文件：
  - `VERSION`
  - `version.json`
  - `src/__init__.py`

## v1.0.0
- 时间：2026-03-18 18:22
- 类型：基线版本 / 开源演示版初始化
- 内容：
  - 从私有开发环境导出独立的开源演示副本
  - 保留核心源码、必要配置、公开 schema、精简文档与 smoke 测试
  - 移除主仓数据库、缓存、日志、报告、抓取结果与历史运行资产
  - 修正副本中的硬编码路径，使其基于副本根目录运行
  - 增加最小依赖清单与可选增强依赖清单
  - 增加一键安装脚本、一键安装快捷方式与演示运行脚本
  - 增加 `README.md`、`THIRD_PARTY_NOTICES.md`、`VERSION`、`version.json`
- 验证结果：
  - `scripts/demo_showcase.py` 在副本虚拟环境中运行通过
  - `tests/test_smoke_demo.py` 通过，结果为 `4 passed`
- 版本文件：
  - `VERSION`
  - `version.json`
  - `src/__init__.py`
- 说明：
  - 该版本是开源演示基线，不等于主开发仓生产态完整快照
  - 默认不附带任何第三方站点抓取结果与运行数据库
  - 当前正式许可证为 `MIT`
  - 当前 Git 标签锚点为 `v1.0.0`
